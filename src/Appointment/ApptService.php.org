<?php
namespace Cookbook\Appointment;
use PDOStatement;
use InvalidArgumentException;
use Cookbook\Appointment\ApptRepository;
class ApptService
{
    public function __construct(public PDO $pdo) {}
    public function list(string $start_date, string $end_date) : ?PDOStatement
    {
        // build SQL SELECT statement using $repo->buildCols()
        // add a WHERE clause the returns "appointment" table entries including and between start and end dates
        // return a PDOStatement instance or null
    }
}
